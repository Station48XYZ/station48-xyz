import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: "https://studio48.xyz",
    image: {
        domains: ["status.mclive.eu"]
    }
});
